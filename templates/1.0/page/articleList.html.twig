{% extends '1.0/base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row pb-2">
            {{ include('1.0/component/article.html.twig') }}
        </div>
        {% set path = 'show-article-list' %}
        {{ include('1.0/component/pagination.html.twig') }}
    </div>
{% endblock %}