{% extends 'colored/base.html.twig' %}

{% block body %}
    {{ encore_entry_link_tags('navbar-colored') }}
    <div class="container-fluid">
        <div class="row justify-content-center text-center flex-column m-4">
            <div class="col-lg-12 mb-5 mt-5">
                <h1>Newsy i nowości na naszym serwerze</h1>
                <h2 class="colored">Sprawdź co nowego u nas...</h2>
            </div>
            {{ include('colored/component/article.html.twig') }}
            {% set path = 'show-article-list' %}
            {{ include('colored/component/pagination.html.twig') }}
        </div>
    </div>
{% endblock %}